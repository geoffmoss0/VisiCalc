#include <stdlib.h>

bool run_func(char *input) {
	
}

trie_node search(char *input, trie_node root) {
	if (
}

bool contains(trie_node check) {
	
	trie_node *list[27] = check->children;
	for (int i = 0; i < 26; i++) {
		if (list[i] != NULL)
	}
}


///This will happen exactly once and fully create the trie
///No modification of the trie should occur, nor should another trie be created
///This will be hardcoded because there will be no reason for the commands to change
trie_node create_trie() {

}
